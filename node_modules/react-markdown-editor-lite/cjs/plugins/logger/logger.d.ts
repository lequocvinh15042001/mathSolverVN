/**
 * logger: undo redo
 */
declare class Logger {
    private record;
    private recycle;
    initValue: string;
    push(val: string): number;
    get(): string[];
    getLast(): string;
    undo(skipText?: string): string;
    redo(): string;
    cleanRedo(): void;
    getUndoCount(): number;
    getRedoCount(): number;
}
export default Logger;
